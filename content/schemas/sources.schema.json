{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wirdi.app/schemas/sources.schema.json",
  "title": "Hadith and reference sources",
  "description": "content/sources/sources.json. Every adhkar.source_id points here. Ids are author-assigned and permanent.",
  "type": "object",
  "required": ["sources"],
  "additionalProperties": false,
  "properties": {
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/source" }
    }
  },
  "$defs": {
    "source": {
      "type": "object",
      "required": ["id", "collection", "reference"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "integer", "minimum": 1, "description": "Author-assigned, globally unique, permanent." },
        "collection": { "type": "string", "minLength": 1, "description": "The hadith collection or book this reference belongs to." },
        "reference": { "type": "string", "minLength": 1, "description": "Locator within that collection, e.g. book and hadith number." },
        "grading": { "type": ["string", "null"], "description": "Optional grading of the narration. Omit when unused." },
        "full_text": { "type": ["string", "null"], "description": "Optional full text of the narration. Omit when unused." }
      }
    }
  }
}
